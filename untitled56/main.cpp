#include <iostream>
#include "vector"
#include "math.h"
using namespace std;
class Figura{

    vector<double> boki;
    vector<double> katy;
public:
    void ustaw(vector<double>,)

};
void Figura::ustaw

class Kwadrat::Figura{
    int przekatna = zwroc_boki()[0] * sqrt(2);

    int pole (vector<double >);
    int obw(vector<double >);
};

double Kwadrat::pole(vector<double> boki,int n){
    return pow(boki[n], 2);
}
double Kwadrat::obw(vector<double> boki, int n){
    return boki[n] * 1;
}
int main() {
    kwadrat kw;
    kw.ustaw
    return 0;
}
